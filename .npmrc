# Expo/React Native + Metro 는 pnpm isolated에서 전이 의존성을 못 찾는다.
# 모노레포 전체를 hoisted(npm 스타일 flat)로 둬 Metro·Next·Prisma 모두 안전하게 해석.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
