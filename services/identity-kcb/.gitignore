# ── 절대 커밋 금지 ────────────────────────────────────────────────
# KCB 운영 라이선스(.dat) = PROD 비밀. 유출 시 회원사 계정 오남용 위험.
secret/
*.dat

# KCB 독점 모듈 JAR (배포는 빌드 호스트에 직접 배치)
libs/*.jar

# 빌드 산출물
target/
*.log

# 로컬 환경파일
.env
.env.*
!.env.example
